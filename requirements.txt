# Pilgrim Pipeline — dependencies

# --- config ---
pydantic-settings
python-dotenv

# --- audio task (Sarvam Bulbul v2 TTS → stitch → Cloudflare R2) ---
httpx
tenacity
boto3

# Audio stitching uses Python's stdlib `wave` (no pydub/audioop — works on Python 3.13+).
# MP3 compression is optional and uses ffmpeg if present (Docker: apt-get install -y ffmpeg);
# without ffmpeg the task outputs WAV. Framework deps (fastapi, uvicorn, redis, celery,
# sqlalchemy, psycopg2) will be added when main.py / orchestrator / models are built.
