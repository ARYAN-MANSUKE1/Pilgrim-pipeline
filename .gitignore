.env
__pycache__/
*.pyc

# Runtime state
dashboard/jobs_state.json

# Local tooling / agent config
.claude/
.clinerules/

# Deliverables & heavy artifacts (shared out-of-band, not in the repo)
experiments/
sample_output/
*.pdf
*.zip
*.wav
*.mp3

# per-temple cost ledger (local data, appended by app/tasks/costs.py)
cost_ledger.jsonl

# Google service-account key for the tracking sheet -- never commit
sheet-key.json
*-service-account*.json

# scratch files from the staging->live migration session
_block_h*.txt
_slider_*.txt
_push_rows.json
migrate_run.log
*.mp3

# rumik trial output (audio files, not source)
rumik/out/

# never commit: live DB credentials, auth salts, redis password
wp-config.php
# 258MB of WordPress debug output pulled off the server for diagnosis
debug.log

# Local measurement/scan output -- regenerable by re-running the scans, and
# a point-in-time snapshot of staging rather than source.
scan_staging.json
scan_lengths.json
scan_now.json
measured_*.json
modified_recent.json
work_inventory.json
usable_models.json
ready_10_languages.json
retranslate_needed.json
quality_test_picks.json
quality_compare.json
quality_compare.txt
translation_compare.json
translation_compare.txt

# Sarvam refund correspondence -- business documents containing payment IDs,
# account balances and a draft email. Kept local, not published to the repo.
sarvam-refund-request.md
sarvam-email-final.md
sarvam-email-PASTE.txt
sarvam-api-test-evidence.txt
